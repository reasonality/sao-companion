// Minimal stub — upgrade only if tests exercise code paths depending on this mock's return value.
export const DOMPurify = {
    addHook: () => {},
    removeHook: () => {},
    sanitize: (s) => s,
};
